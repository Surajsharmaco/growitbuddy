{
  "buildCommand": "pnpm --filter @workspace/growitbuddy run build",
  "outputDirectory": "artifacts/growitbuddy/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
